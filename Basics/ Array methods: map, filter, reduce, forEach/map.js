/*1. map() — transforms elements & returns a new array
It runs a function on every element and returns a new array with the results.*/

let numbers = [1, 2, 3, 4];
let doubled = numbers.map((num) => num * 2);
console.log(doubled); // [2, 4, 6, 8]

//Use map() when you want to create a new array based on some transformation.

